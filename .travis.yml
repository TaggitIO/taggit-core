language: ruby

rvm:
  - 2.0.0

bundler_args: --without development production

script:
  - export $TAGGIT_SECRET_TOKEN='foobarbaz'
  - RAILS_ENV=test bundle exec rake db:migrate
  - bundle exec rspec spec

branches:
  only:
    - master
    - dev