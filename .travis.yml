language: ruby

rvm:
  - 2.1.0

bundler_args: --without development production

before_script:
  - RAILS_ENV=test bundle exec rake db:migrate

env: 
  - TAGGIT_SECRET_TOKEN="foobarbaz" MANDRILL_APIKEY='somenonsense'

branches:
  only:
    - master
    - dev
